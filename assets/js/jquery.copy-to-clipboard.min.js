function CopyToClipboard(t) {
    var o = $("#_hiddenClipboard_");
    o.length || ($("body").append('<textarea style="position:absolute;top: -9999px;" id="_hiddenClipboard_"></textarea>'), o = $("#_hiddenClipboard_")), o.html(t), o.select(), document.execCommand("copy"), document.getSelection().removeAllRanges(), o.remove()
}
$.fn.CopyToClipboard = function() {
    CopyToClipboard(this.is("select") || this.is("textarea") || this.is("input") ? this.val() : this.text())
}, $(function() {
    $("[data-clipboard-target]").each(function() {
        $(this).click(function() {
//            $($(this).data("clipboard-target")).CopyToClipboard()
		var t = $(this).attr("clipboard-target");
			void 0 !== t && t && ($(this).html("¡COPIADO!"), CopyToClipboard($(this).data("clipboard-text")), window.setTimeout(function() {
            $("[data-clipboard-text]").html("Copiar")
        }, 1500))
        })
    }),
	 /*$('[data-clipboard-text]').each(function(){
        $(this).click(function(){
            //CopyToClipboard($(this).data('clipboard-text'));
			var t = $(this).attr("data-clipboard-text");
			void 0 !== t && t && ($(this).html("¡COPIADO!"), CopyToClipboard($(this).data("clipboard-text")), window.setTimeout(function() {
            $("[data-clipboard-text]").html("Copiar")
        }, 1500))
        });
    })*/
	$(document).on("click", ".btn--copy", function() {
        var t = $(this).attr("data-clipboard-text");
        void 0 !== t && t && ($(this).html("¡COPIADO!"), CopyToClipboard($(this).data("clipboard-text")), window.setTimeout(function() {
            $("[data-clipboard-text]").html("Copiar")
        }, 1500))
    }),
	$(document).on("click", "#boton-copy", function() {
        var t = $(this).attr("data-clipboard-text");
        void 0 !== t && t && ($(this).html("¡COPIADO!"), CopyToClipboard($(this).data("clipboard-text")), window.setTimeout(function() {
            $("[data-clipboard-text]").html("Copiar")
        }, 1500))
    })
});
